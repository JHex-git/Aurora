set(TARGET Scene_lib)
file(GLOB_RECURSE SRCS *.cpp)
add_library(${TARGET} STATIC ${SRCS})
target_link_libraries(${TARGET} PRIVATE assimp tinyxml2 stb)
target_link_libraries(${TARGET} PUBLIC Render_lib Math_lib)