file(GLOB SRCS *.cpp)
set(TARGET Editor_lib)
add_library(${TARGET} OBJECT ${SRCS})
target_link_libraries(${TARGET} PRIVATE imgui glfw)
target_link_libraries(${TARGET} PUBLIC Runtime_lib)
if (MSVC)
    set_target_properties(${TARGET} PROPERTIES FOLDER "Engine")
endif()